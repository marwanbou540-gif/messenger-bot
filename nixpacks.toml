[phases.setup]
nixPkgs = ["python3", "python3-pip", "ffmpeg", "git"]

[phases.build]
cmds = ["npm install"]

[buildConfig]
env = { 
  PYTHON = "python3",
  PYTHON3 = "python3",
  PYTHONUNBUFFERED = "1"
}

[start]
cmd = "node index.js"
